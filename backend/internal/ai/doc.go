// Package ai contains FlowCast's AI pipeline: the provider-agnostic Analyzer
// interfaces, prompt templates and their versions, structured-output parsing, and the
// OpenAI and mock implementations. Nothing outside this package should know which model
// vendor is in use.
package ai
